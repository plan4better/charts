{{- if .Values.windmill.workers.print.enabled -}}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "goat.serviceFullname" (dict "context" . "component" "windmill-worker-print") }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "goat.serviceLabels" (dict "context" . "component" "windmill-worker-print") | nindent 4 }}
data:
  {{- range $k, $v := .Values.windmill.workers.print.config }}
  {{ $k }}: {{ $v | quote }}
  {{- end }}
{{- end }}
