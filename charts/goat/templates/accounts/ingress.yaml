{{- if and .Values.accounts.enabled .Values.accounts.ingress.enabled -}}
{{- $component := "accounts" -}}
{{- $ctx := dict "context" . "component" $component -}}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "goat.serviceFullname" $ctx }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "goat.serviceLabels" $ctx | nindent 4 }}
  {{- with .Values.accounts.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- with .Values.accounts.ingress.className }}
  ingressClassName: {{ . }}
  {{- end }}
  {{- with .Values.accounts.ingress.tls }}
  tls:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  rules:
    {{- range .Values.accounts.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            pathType: {{ .pathType }}
            backend:
              service:
                name: {{ include "goat.serviceFullname" $ctx }}
                port:
                  number: {{ $.Values.accounts.service.port }}
          {{- end }}
    {{- end }}
{{- end }}
