{{- if .Values.catalog.enabled -}}
{{- $component := "catalog" -}}
{{- $ctx := dict "context" . "component" $component -}}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "goat.serviceFullname" $ctx }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "goat.serviceLabels" $ctx | nindent 4 }}
  {{- with .Values.catalog.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.catalog.service.type }}
  ports:
    - port: {{ .Values.catalog.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "goat.serviceSelectorLabels" $ctx | nindent 4 }}
{{- end }}
